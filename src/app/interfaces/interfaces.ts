export interface Universidad {
    icon: string;
    name: string;
    redirectTo: string;
}

export interface Facultad {
    icon: string;
    name: string;
    redirectTo: string;
}

export interface Carrera {
    img: string;
    name: string;
    redirectTo: string;
}
